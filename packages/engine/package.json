{
  "name": "@big-two/engine",
  "version": "0.0.0",
  "description": "Pure, deterministic, framework-agnostic Big Two rules engine. No DOM/React imports allowed.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
