// The animation features, in a file of their own so they load after the menu
// is on screen: nothing on the menu moves, and cards are what they are for.
export { domAnimation as default } from 'framer-motion';
