{
  "name": "@big-two/campaign",
  "version": "0.0.0",
  "description": "The single-player campaign: Worth, pots and stakes, classes and their passives, the ladder of tables, Medallions and Vestiges. Pure rules and state; the base game is untouched.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@big-two/ai": "*",
    "@big-two/engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.16.2",
    "@types/node": "^22.5.0",
    "vitest": "^2.0.5"
  }
}
