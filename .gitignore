# Dependencies and build output
node_modules/
dist/
*.tsbuildinfo
coverage/

# Local environment. TURN credentials belong in repository secrets, not here.
.env.local
.env.*.local
.env.production

# Editors and operating systems
.DS_Store
Thumbs.db
*.log

# Claude Code's own files: kept on the machine that uses them, not published.
.claude/
skills-lock.json
